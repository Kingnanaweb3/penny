# Phase 4 — Fix Log

| Finding | Test file | Failed with | Change made | Passes now |
|---------|-----------|-------------|-------------|------------|
| P1 — Float money | `tests/penny/p1-float-money.test.js` | (1) `Entry "processing fee" amountCents=undefined — not a whole-cent integer`; (2) `ledger.balanceCents is not a function` | `src/ledger.js` — `post()` now converts dollars to integer cents via `Math.round(amount * 100)` and stores `amountCents`; `balance()` sums `amountCents` and divides by 100 on return; new `balanceCents()` returns the integer sum directly. No change to `payments.js` or `invoice.js`. | yes |
| P2 — Rounding drift | `tests/penny/p2-rounding-drift.test.js` | `chargeTotal ($322.52) must equal receiptTotal ($322.51); drift = $0.01` — `322.52 !== 322.51` | `src/invoice.js` — removed per-line `round2()` in `chargeTotal`. Introduced `totalWithVat(items)`: converts subtotal to integer cents (`Math.round(subtotal * 100)`), applies sales tax in cents (`Math.round(subtotalCents * 1.075)`), returns cents ÷ 100. Both `receiptTotal` and `chargeTotal` now delegate to this single function — one subtotal, one rounding, tax calculated in whole cents. Rule 2 now reads: "Sales tax is 7.5 percent, calculated once on the invoice subtotal and rounded once to the nearest cent. The amount charged must equal the amount shown on the customer receipt." | yes |
| P3 — Missing idempotency | `tests/penny/p3-idempotency.test.js` | (1) `Customer was debited $215.00 but should have been debited $107.50 (exactly once)` — `215 !== 107.5`; (2) `Retry added 2 ledger entries; expected 0` — `4 !== 2` | `src/payments.js` — added `this.idempotencyKeys = new Map()` to the constructor. At the top of `charge()`, if `idempotencyKey` is present and already in the map, return the stored result immediately without posting anything. After a successful first charge, store `result` in the map under its `idempotencyKey`. | yes |

## Notes on existing tests

No existing test in `tests/payments.test.js` was changed. The test `"fee is 1.5 percent of the charge"` asserts `r.fee === r.amount * 0.015`. This still holds because `PaymentService.charge()` still computes and returns the fee in dollars as `amount * FEE_RATE`; only the ledger's internal storage changed. That test is asserting buggy fee behavior (P4 will fix it later) — it is left intact per the audit rules and will be updated when P4 is fixed.
